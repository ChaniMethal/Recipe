<configuration>
	<connectionStrings configSource="App.secret.config" />
</configuration>

<!--connection strings should be in App.secret.config which is git ignored
the secret.config should have a connectionstring called devconn-->

